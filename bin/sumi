#!/usr/bin/env bash

if [[ -z "$SUMI_DIR" ]]
then
  >&2 echo "To run SUMI first set the variable \$SUMI_DIR"
  >&2 echo "    export \$SUMI_DIR=/path/to/sumi"
  exit
fi
  
python $SUMI_DIR/src/sumi.py $@
